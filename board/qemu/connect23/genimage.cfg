image efi-part.vfat {
	vfat {
		label = "ESP"
	}

	empty = true
	size = 128M
}

image disk.img {
	hdimage {
		partition-table-type = "gpt"
	}

	partition boot {
		image = "efi-part.vfat"
		partition-type-uuid = U
		offset = 32K
	}
}
